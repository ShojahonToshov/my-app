import { createClient } from "@supabase/supabase-js"; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); supabase.from("bookings").select("id").limit(1).then(({data}) => { if (data && data.length) { supabase.from("bookings").update({ reviewText: "Test review" }).eq("id", data[0].id).select().then(console.log); } else { console.log("No bookings"); } });
