import { createClient } from "@supabase/supabase-js"; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); supabase.from("bookings").insert({ time: "10:00", status: "completed" }).select().then(({data, error}) => { if (error) console.log(error); else { console.log(data); supabase.from("bookings").update({ reviewText: "Test review", rating: 5 }).eq("id", data[0].id).select().then(console.log); } });
