import { createClient } from '@supabase/supabase-js'; const supabase = createClient('https://hhlbquuxnesiuwknscyq.supabase.co', process.env.SUPABASE_SERVICE_ROLE_KEY); const run = async () => { const { data, error } = await supabase.rpc('execute_sql', { sql_query: 'SELECT * FROM pg_policies WHERE tablename = \\'bookings\\'' }); console.log('DATA:', JSON.stringify(data)); process.exit(0); }; run();
