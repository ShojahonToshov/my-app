import { createClient } from "@supabase/supabase-js"; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.from("businesses").select("id, name, rating, reviews_count"); console.log("Businesses:", data); const { data: bookings } = await supabase.from("bookings").select("id, client_id, business_id, rating, reviewText"); console.log("Bookings:", bookings); } run();
