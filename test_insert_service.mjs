import { createClient } from '@supabase/supabase-js'; const supabase = createClient('https://hhlbquuxnesiuwknscyq.supabase.co', process.env.SUPABASE_SERVICE_ROLE_KEY); const run = async () => { const { data, error } = await supabase.from('bookings').insert([{ business_id: '11111111-1111-1111-1111-111111111111', service_id: '1', date: '2026-08-20', time: '10:00', guest_name: 'Test', guest_phone: '12345', is_guest: true, status: 'pending' }]); console.log('DATA:', data); console.log('ERROR:', JSON.stringify(error)); process.exit(0); }; run();
